package constant

const (
	// -- ERRORS --
	// connection
	RDConnectionError       = "Cannot connect to the RD database!"
	RDURLParseError			= "Cannot parse redis connection URL!"

	// -- DEBUG --
	RDConnectionHealthy = "Connection to redis is healthy."
)
